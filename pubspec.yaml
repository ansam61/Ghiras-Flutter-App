name: ghiras_app
description: Ghiras AI Plant Care and Diagnosis Mobile Application
publish_to: 'none'
version: 1.0.0+1

# ========================================================
# Ghiras .NET 10 Web API Backend Configuration
# Primary Web/Desktop Endpoint: http://localhost:5250/api
# Android Emulator Endpoint: http://10.0.2.2:5250/api
# Secure HTTPS Endpoint: https://localhost:7267/api
# ========================================================

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  http: ^1.2.0
  google_fonts: ^6.1.0
  cupertino_icons: ^1.0.6
  provider: ^6.1.1
  intl: ^0.20.2
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
